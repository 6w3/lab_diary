---
description: Lab Diary project instructions (Czech replies, deploy via vps submodule)
alwaysApply: true
---

# Lab Diary

- Czech replies; English code. Only do what user asked. Never commit/push unless asked.
- No mock lab data when extract fails — fix real pipeline.
- Upload-first + Smart NVIDIA default; catalog marker match when possible; custom OK otherwise.
- Ship flow when asked: push `lab_diary` → bump `vps` submodule `services/lab_diary` → commit message `update lab diary` → push `vps` **only if** that submodule pointer is the sole change.
- Multi-date: spaced Czech dates; classic table wins if it finds more dates than Smart.
- Full detail: see `AGENTS.md` in repo root.
